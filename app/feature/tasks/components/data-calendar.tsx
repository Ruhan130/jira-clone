import { Task } from "../types";

interface DataCalendarProps {
    data: Task[]
}

export const DataCalendar = ({
    data    
}: DataCalendarProps) => {
    return (
        <div>
            Data CaLendar
        </div>
    )
}